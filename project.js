$(document).ready(function(){
    $(".slider").slick({
        autoplay: true,
        autoplaySpeed: 2000,
        dots: true,  // Shows dots (without numbers)
        arrows: false,
    });
});
